const { createCustomerMessages, getCustomerMessages, updateCustomerMessages } = require("./sofia-van-flopper.repository");

module.exports = {
    createCustomerMessages,
    getCustomerMessages,
    updateCustomerMessages
}