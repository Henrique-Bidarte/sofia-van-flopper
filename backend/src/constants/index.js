const { API_PORT } = require("./server.constant")
const { ROUTER, ROUTER_ENDPOINTS } = require("./routes.constant")
const { DATABASE } = require("./sofia-van-flopper-database.constant")

module.exports = {
  API_PORT,
  ROUTER,
  ROUTER_ENDPOINTS,
  DATABASE
}
