const ROUTER = {
  SOFIA_VAN_FLOPPER: "/sofia-van-flopper",
};

const ROUTER_ENDPOINTS = {
  HEALTH_CHECK: "/healthcheck",
  CATALOG: "/catalog"
};

module.exports = {
  ROUTER,
  ROUTER_ENDPOINTS,
}
